-- Create ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "MyGui"
screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Create a Frame for layout
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 400, 0, 250)
frame.Position = UDim2.new(0.5, -200, 0.5, -125)
frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
frame.BackgroundTransparency = 0.5
frame.Parent = screenGui

-- Create a TextButton to speed up carts
local button = Instance.new("TextButton")
button.Size = UDim2.new(0, 250, 0, 50)
button.Position = UDim2.new(0.5, -125, 0.5, -25)
button.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
button.Text = "Speed Up All Carts!"
button.TextColor3 = Color3.fromRGB(255, 255, 255)
button.Font = Enum.Font.SourceSansBold
button.TextSize = 24
button.Parent = frame

-- Create an "X" button to close the GUI
local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 50, 0, 50)
closeButton.Position = UDim2.new(1, -60, 0, 10)
closeButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
closeButton.Text = "X"
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.Font = Enum.Font.SourceSans
closeButton.TextSize = 24
closeButton.Parent = frame

-- Function to speed up all carts with dramatic effects
local function onButtonClick()
    local speedIncrease = 3
    local cartCount = 0

    for _, cart in pairs(workspace:GetChildren()) do
        if cart:IsA("Model") then
            local upButton = cart:FindFirstChild("Up")
            if upButton and upButton:IsA("Part") then
                local clickPart = upButton:FindFirstChild("Click")
                if clickPart then
                    local chngScript = clickPart:FindFirstChildWhichIsA("Script")
                    if chngScript then
                        local chngFunction = chngScript:FindFirstChild("chng")
                        if chngFunction then
                            chngFunction:Invoke() -- Invoke the speed change function
                            cartCount += 1
                        end
                    end
                end
            end
        end
    end

    -- Dramatic feedback
    if cartCount > 0 then
        button.Text = "Carts Speeding Up!"
        button.BackgroundColor3 = Color3.fromRGB(255, 255, 0)
        wait(1)
        button.Text = "Speed Up All Carts!"
        button.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    else
        button.Text = "No Carts Found!"
        button.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        wait(1)
        button.Text = "Speed Up All Carts!"
        button.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    end
end

-- Function to close the GUI
local function onCloseButtonClick()
    screenGui:Destroy() -- Remove the GUI
end

-- Connect the button clicks to their functions
button.MouseButton1Click:Connect(onButtonClick)
closeButton.MouseButton1Click:Connect(onCloseButtonClick)
